{
  "name": "aituber-flow-dev",
  "private": true,
  "workspaces": [
    "landing",
    "app"
  ],
  "scripts": {
    "dev:landing": "npm run dev -w landing",
    "dev:app": "npm run dev -w app",
    "build:landing": "npm run build -w landing",
    "build:app": "npm run build -w app",
    "build": "npm run build:landing && npm run build:app"
  }
}
